# React + Vite

This project uses Vite with React and is set up to build and deploy with npm.

## Development

- Install dependencies: npm install
- Start the dev server: npm run dev
- Build for production: npm run build
- Preview the production build: npm run preview

The frontend is configured for Vercel deployment through the project root Vercel settings.
