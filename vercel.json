{
    "installCommand": "uv pip install -r requirements.txt && cd client && npm ci",
    "buildCommand": "cd client && npm run build",
    "outputDirectory": "client/dist",
    "functions": {
        "api/index.py": {
            "includeFiles": "api/fallback_data/**"
        }
    }
}
